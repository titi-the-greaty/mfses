# ============================================================
# SeeSaw MFSES — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to git (it's in .gitignore).
# ============================================================

# --- Supabase ---
# Found at: supabase.com → Your Project → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...your-service-role-key-here

# --- Polygon.io ---
# Sign up at polygon.io → Dashboard → API Keys
# Free tier: 5 calls/min | Starter $29/mo: unlimited
POLYGON_API_KEY=your-polygon-api-key-here

# --- GitHub Secrets (set in repo Settings → Secrets) ---
# These are NOT in .env — they go in GitHub repo settings:
# SUPABASE_ACCESS_TOKEN    → supabase.com → Account → Access Tokens
# SUPABASE_PROJECT_REF     → supabase.com → Project → Settings → General → Reference ID
# SUPABASE_DB_PASSWORD     → The password you set when creating the project

# --- n8n Environment Variables ---
# Set these in n8n → Settings → Variables:
# SUPABASE_URL             → same as above
# SUPABASE_SERVICE_KEY     → same as SUPABASE_SERVICE_ROLE_KEY above
# POLYGON_API_KEY          → same as above

# --- Vercel ---
# No env vars needed for Vercel (dashboard is static HTML).
# Just connect your GitHub repo at vercel.com/new.
# Set the "Root Directory" to: dashboard/
# That's it — auto-deploys on every push.

# --- Alert Config (in n8n) ---
# ALERT_EMAIL=your-email@gmail.com
# SMTP credentials configured in n8n credential manager
